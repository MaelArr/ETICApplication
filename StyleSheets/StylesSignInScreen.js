import {StyleSheet} from "react-native";

  const styles = StyleSheet.create({
    text: {
      fontSize : 100,
      fontWeight: "bold",
      fontStyle : "italic"
    }
  });

  export default styles;